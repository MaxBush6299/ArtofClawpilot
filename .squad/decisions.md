# Decisions

## 2026-04-27

- First Azure Container Apps Job invocation succeeded through MAI image generation but failed at `git push` with `Permission to MaxBush6299/ArtofClawpilot.git denied ... exitCode 128`. Root cause: GitHub App installation lacks Contents: Read & Write permission. Clone succeeded (proving Contents: Read exists); push failed (proving Contents: Write missing).
- Team consensus on recovery: Fix GitHub App permissions by granting Contents: Read & Write, then rerun smoke proof on disposable `hosted-smoke` branch per documented safety stance (docs/architecture/hosted-smoke-checklist.md). Do not rerun against main until smoke proof passes on branch.
- Hosted smoke architecture requires three phases on disposable `hosted-smoke` branch: Phase A (auth/wiring dry-run), Phase B (durable publish + idempotent rerun), Phase C (failure-path proof). No smoke commits land on main during proof. Butters reviews ACA logs + smoke branch commits before sign-off.
- Architecture decision reaffirmed: direct push to `main` is the correct design (not PR-based). Single-transaction publish contract (one commit per `runDate`) preserves design intent and one-commit-per-day invariant. PR-based fallback deferred unless branch protection explicitly blocks app.
- Documentation gap: Add GitHub App permission prerequisites to docs/architecture/hosted-daily-run.md as setup requirement section, explicitly listing Contents: Read & Write as required for git push to main and Metadata: Read-only as automatically included.
- Smoke proof checkpoint: After Phase B proof passes on hosted-smoke branch with idempotent rerun validation, production cutover resumes with intended direct-push-to-main design. Permissions fix and branch policy form the immediate safe next step.
- Frontend guide decision: Wendy drafted `docs/architecture/frontend-hosted-update-guide.md` with preliminary sign-off identifying zero code changes needed for Home.tsx and Room.tsx. Frontend already defensively handles `skipped[]` at top level and optional audit fields; both pages use optional chaining and truthy checks.
- Frontend guide review (rejected): Butters identified five inaccuracies requiring Stan revision: (1) `runDate` overstated as full ISO 8601 instead of `YYYY-MM-DD` day key; (2) Room.tsx criticism rendering misdescribed as optional chaining vs. actual `{img.criticism && (...)}`; (3) static JSON import overclaimed as TypeScript build-time schema validation; (4) Home cover incorrectly assumed to be newest image despite pre-existing images; (5) contract boundaries conflated data validation with frontend tolerance.
- Frontend guide decision (approved): Stan revised `docs/architecture/frontend-hosted-update-guide.md` with all corrections verified against Home.tsx actual behavior, Room.tsx rendering, orchestrator append semantics, and hosted contract specs. Butters approved after verification. Approval scope: frontend-guide accuracy for smoke practicality and `skipped[]` tolerance; does not supersede broader hosted smoke gates in `hosted-smoke-checklist.md`.
- Live ACA Job execution on main (2026-04-27T15:17-15:18Z): Tolkien triggered production ACA Job (artclaw-daily-job-2pewehrfleuls-j3e3a2s) with GitHub App Contents:Write permission fix applied. Result: 79 seconds end-to-end success, generated "Primordial Coalescence" with MAI-Image-2e-1, committed a57c085117e892e4042743994e9b68c3e8f03d84, Log Analytics showed complete phase progression (bootstrap → Curator → Artist → image gen → validation → git push). Call counts: Curator (1 reasoning), Artist (3 reasoning + 1 image), Critic (0, skipped). Write set: gallery.json, next-brief.json, PNG asset. **Permission fix verdict:** Contents:Write grant successfully resolved prior exitCode 128 push failure. Happy-path orchestration and infrastructure wiring all confirmed working.
- Live ACA smoke promotion gate assessment: Butters reviewed Tolkien's main-branch execution and split verdict: (1) permission fix confirmed working — no further debugging needed, (2) smoke promotion gate NOT satisfied — run violated documented architecture by pushing to main instead of disposable `hosted-smoke` branch. Missing proof: Phase B idempotent rerun validation (same runDate must exit `already_resolved` zero-call no-op on second execution), Phase C failure-path proof (structured skip scenario), `hosted-smoke` branch isolation safety. Recommendation: acknowledge permission fix success; do NOT enable scheduled production until Phase A/B/C smoke proof completes on correct branch per `docs/architecture/hosted-smoke-checklist.md`. Next step: create `hosted-smoke` branch, execute three-phase smoke proof, collect Log Analytics evidence, then promote to scheduled main execution after Butters sign-off.
- Multi-run invariant architecture shift: Max requested API-triggered manual runs to enable multiple images per day beyond the scheduled daily run. Current architecture enforces exactly one image OR one skip per `runDate`. Proposed shift: introduce `runId` (unique run identifier) as primary idempotency key, while keeping `runDate` as grouping field. Scheduled runs use `runId=scheduled-{runDate}` (one per day); manual runs use `runId=manual-{runDate}-{uuid}` (multiple per day allowed). Schema changes: add `runId` and `triggerSource` to `GalleryImageRecord` and skip records. Asset paths include `runId` to prevent collision. Idempotency check changes from "does `runDate` exist?" to "does `runId` exist?". Multiple images per day share `runDate` but have distinct `runId` values. Frontend already handles multiple images per day (no code changes needed). Skip records still close the `runDate` (one skip per day, no multi-skip). Rate limits and API auth defined at API layer. Backward compatible: old images without `runId` default to `id`. Rollout sequence: (1) add `runId` field, (2) update idempotency check, (3) update bootstrap shell for scheduled runs, (4) deploy and verify with smoke proof, (5) build API endpoint with auth and rate limits.
- Multi-run idempotency contract design: Kyle designed backend contract for multi-run support using composite run identity. Run identification: each execution gets unique `runId` (format `{runDate}-{triggerSource}-{seq}` for readability). Trigger sources: `scheduled` (daily cron), `manual-api` (HTTP endpoint), `manual-cli` (admin override). Image identification: enhanced to `{runDate}-{imageName}` with collision suffix if needed. Skip record identity: `skip-{runId}`. Data shape: `GalleryImageRecord` gains `runId` and `triggerSource` fields (optional initially, required after Phase 3). Idempotency resolution: check `runId` exact match → no-op if found; new `runId` same `runDate` → proceed with generation; supports `allowMultiple=false` flag to reject multi-run. Retry behavior: same `runId` rerun = no-op; new `runId` same day = new generation; skip retryable via new `runId`. Git strategy: one commit per successful publish, multiple commits per day when multiple runs succeed. Backward compatible: Phase 1 (add `runId`, optional), Phase 2 (API route), Phase 3 (enforce uniqueness). Migration preferred format: `{runDate}-{source}-{seq}` for log readability; skip commits separate for audit trail; GitHub App extended for API permissions.
- API rollout lift sizing analysis: Kyle sized two implementation options for manual API trigger. Option 1 (Orchestrator-only, 2–3 days): add `runId` contract, update idempotency, validate multi-run, no infra changes. Option 2 (Full API integration, 1–2 weeks): Option 1 + Azure Function App, auth layer, ACA Job invocation, rate limiting, execution tracking. Key decisions: direct ACA Job invocation (not enqueue-first) because Job has built-in retry and queue adds little value; lock-free concurrency (orchestrator resolves via `runId` rather than advisory lock) because idempotency check is atomic and lock adds failure modes; API pre-checks `data/gallery.json` from GitHub before invocation to provide immediate feedback and prevent wasted job executions. Code touchpoints: `orchestrator/contracts.py` + `validation.py` + `main.py` for Option 1; new `api/trigger-run/*` + `infra/modules/apiFunction.bicep` for Option 2. Recommended sequence: prove Option 1 with manual CLI triggers first, add Option 2 behind feature flag once gates pass. Cost estimate: ~$0.04/month incremental (negligible). Hybrid approach recommended: start with Option 1, demonstrate multi-run safety, then build API layer incrementally.
- Sequential API rollout path (Path 1, recommended): Stan recommends sequential implementation over parallel. Path 1 sequence: Phase 1a–1d refactor idempotency (Kyle + Tolkien + Butters review, ~7 days), then Phase 2a–2d build API layer (~15 days). Total ~15–22 days elapsed. Advantages: smaller proof scope (scheduler and API validated separately), incremental confidence signal (scheduler can promote independently), clearer team parallelization (Kyle → Tolkien → Butters → API layer), easier rollback (scheduler live even if API smoke fails), risk mitigation (each phase has independent validation gate). Path 2 (parallel) work in parallel but has higher integration risk (both-or-nothing promotion), combined test matrix explosion (15+ scenarios), design-to-implementation mismatch discovered late. Path 1 Phase 1d gates: scheduled run on 2026-04-27 generates `scheduled-2026-04-27` image and commits; same-day rerun exits `already_resolved` with zero calls. Path 1 Phase 2d gates: API run generates `manual-{date}-{uuid}`, same day second manual generates distinct image, both persist correctly, concurrent runs don't corrupt gallery, git push handles multi-commit conflicts. Estimated full deployment: late June (two proof cycles plus buffer). Next steps for Max: confirm Path 1 acceptance, define API scope (rate limits, auth, room assignment), align Phase 1d criteria with Butters, plan Phase 2a API design after Phase 1d sign-off.
- Multi-run testing gates specification: Butters defined comprehensive testing matrix for multi-run support. Testing gates required: (1) duplicate request rejection (same `runId` twice → second exits `already_resolved` zero-call no-op), (2) multi-publish same day (different `runId` same `runDate` → both publish), (3) scheduled + manual coexistence (daily scheduler and API manual on same day), (4) skip closes day (skip prevents retry), (5) concurrent API safety (two simultaneous requests complete without corruption), (6) git push retry (conflict on push → rebase-retry succeeds), (7) frontend tolerance (pages handle multiple images same `runDate`), (8) log correlation (distinct `traceId`/`requestId` per run). Acceptance criteria: 10 acceptance tests (MR-1 through MR-10) pass local dry-run, extended hosted smoke (Phase B/C with multi-run scenarios) passes, concurrent execution proof, Wendy frontend sign-off, Log Analytics per-request correlation. Rejection criteria: duplicate `runId` in gallery, skip-then-retry creates image, concurrent runs corrupt `gallery.json`, git push retry fails, frontend crashes with multi-`runDate`, logs missing `requestId`. Rollout recommendation: Phase 1 (manual-only multi-run) for 30+ days before Phase 2 (multi-scheduled-runs with auto-generated `requestId`). Open questions: API caller provides `requestId` or auto-generate, asset naming includes `requestId` suffix, skip retry escape hatch policy, Phase 2 multi-scheduled scope now or defer, per-day rate limit (e.g., max 5 images).
- **Issue #16 validation gates (closed):** Butters established 20-test acceptance matrix for run identity refactor. Categories: Scheduled Run Safety (SR-1–SR-3), Manual Run Safety (MR-1–MR-4), Skip-Closes-Day (SK-1–SK-3), Asset Collision Prevention (AC-1–AC-3), Frontend Tolerance (FT-1–FT-3), Log Correlation (LC-1–LC-3), Hosted Deployment Gate (HG-1–HG-4). Identity shift: from single `runDate` to composite (`runDate`, `runId`). Scheduled format: `runId=scheduled-{runDate}` (one per day); Manual format: `runId=manual-{runDate}-{uuid}` (multiple per day). Rejection criteria: duplicate (`runDate`, `runId`) pairs, skip-then-retry bypass, asset collision, frontend build failure, deployed pipeline failure. Approval requires all 20 tests passing, extended orchestrator proof, frontend build green, hosted smoke Phase B/C extended proof, deployed pipeline success with gallery image. Close gate evidence: deployed ACA Job execution ID, gallery record with `runId` field, asset PNG with `runId`-suffixed filename, commit SHA to main, Log Analytics KQL proof, validation logs showing idempotent retry. **Ticket resolved 2026-04-27:** Kyle implemented initial contract (6e82793, rejected); Stan fixed blocking defect in image_id generation (f425cae, approved); Tolkien executed deployed validation generating "Inaugural Hall: Dawn of Creation" (execution `artclaw-daily-job-2pewehrfleuls-fgp1l2d`, commit `e5c07c507251d4fefccfa14002e70d51ab3a2d14`, Log Analytics workspace `artclaw-logs-2pewehrfleuls`). All close gates satisfied. Infrastructure now ready for multi-run support (API endpoint scope deferred).
- **Issue #16 implementation review (rejected, 2026-04-27):** Butters rejected Kyle's commit 6e82793. Blocking defect: `orchestrator/main.py:375` used `id=context.run_date` instead of `id=context.run_id`, causing duplicate image_id errors on same-day multi-run attempts. Evidence: local dry-run with `--run-id manual-2099-05-01-xyz789` after existing `scheduled-2099-05-01` publish failed with `image_id_duplicate` exit code 11. Acceptance test MR-2 failed. Positive findings: `runId` fields added to contracts, validation checks `runId` for duplicate, CLI accepts `--run-id`, bootstrap injects `RUN_ID`, frontend build passes, logs include `runId`. Reviewer lockout: Kyle locked out; fix assigned to Stan or Tolkien.
- **Issue #16 re-review approval (2026-04-27):** Butters approved Stan's fix (commit f425cae). Stan's revision surgically fixed blocking defect with three targeted changes in `orchestrator/main.py:375-376`: (1) changed `id=context.run_date` to `id=context.run_id`; (2) added `run_id_suffix` extraction for asset path uniqueness; (3) updated asset paths to include runId suffix pattern. Verification: `git diff 6e82793 f425cae` shows four-line surgical fix; no remaining `id=context.run_date` patterns; frontend build exits 0; validation logic correctly checks `effective_run_id()` and `skip.run_id`; skip records use `id=f"skip-{run_id}"` format; CLI accepts `--run-id`; bootstrap injects `RUN_ID`; log correlation includes `runId`. Commit message documents validation proof with distinct asset filenames for same-day runs. **Next step:** Tolkien proceeds to deployed pipeline close gate per `docs/architecture/issue-16-deploy-gate.md`. Reviewer lockout: Kyle locked out; Stan available for future revisions. Evidence required before issue close: deployed ACA Job execution hash, image generation with distinct runId, Log Analytics runId correlation, git commit SHA to main, idempotent retry validation (same runId exits `already_resolved` with 0 calls), asset path proof with runId suffix.
- **Issue #16 architecture alignment (2026-04-27):** Stan refactored four architecture docs to support run-identity-scoped idempotency (primary key shift from `runDate` to `runId`). Changes propagated through `hosted-daily-run.md` (problem statement, invariant section, execution contract, log fields), `hosted-execution-plan.md` (validation notes), `hosted-validation-gates.md` (release gates, validation categories, dry-run coverage, promotion bar), `hosted-smoke-checklist.md` (Phase B idempotent-rerun proof), and `frontend-hosted-update-guide.md` (multi-image-per-runDate tolerance). Terminology shift: "one-outcome-per-day" → "run-identity-scoped idempotency"; "day-scoped" → "run-identity-scoped". Recommended `runId` formats: scheduled `scheduled-{YYYY-MM-DD}`, manual `manual-{YYYY-MM-DD}-{uuid-short}`. Asset path pattern: from `YYYY-MM-DD-{slug}.png` to `YYYY-MM-DD-{runId-suffix}.png`. Validation changes: check `runId` uniqueness before `runDate`; add skip-closes-day check; return `already_resolved` for `runId` duplicate, `day_already_closed` for skip. Commit 461e5ed, no file conflicts with active team members.
- **Issue #16 deployment gate architecture (2026-04-27):** Tolkien proposed deployed pipeline close gate for run identity refactor. Infrastructure changes: add Bicep parameters `hostedRunId` and `hostedTriggerSource`; add `RUN_ID` and `TRIGGER_SOURCE` environment variables to job template; default `RUN_ID` to `scheduled-{runDate}` if not provided; pass to orchestrator CLI `--run-id` and `--trigger-source`. Bootstrap updates: inject `RUN_ID` and `TRIGGER_SOURCE` into orchestrator env; default both if not provided; include in bootstrap log context. Orchestrator CLI contract: `python3 -m orchestrator.main --repo-root "$REPO_WORKSPACE" --run-date "$RUN_DATE_UTC" --run-id "$RUN_ID" --trigger-source "$TRIGGER_SOURCE"`. Evidence requirements: Log Analytics shows `runId` field in bootstrap and orchestrator logs; git commit contains gallery record with `runId` and `triggerSource`; idempotent rerun with same `runId` exits as `already_resolved`; asset paths do not collide for same-day runs. Validation sequence: pre-flight verify Kyle implementation, infra update with new parameters, smoke execution on `hosted-smoke` branch, production validation on main, evidence collection via logs/commits. Alternatives rejected: pass runId only through bootstrap (violates separation of concerns); use HOSTED_TRACE_ID as runId (different purpose, wrong structure); infer runId from commit metadata (circular dependency). Risks: bootstrap default conflicts with orchestrator (mitigation: agree on format, document in gate); existing executions break if args required (mitigation: make args optional initially); multi-run same-day requires complex concurrency proof (mitigation: defer concurrent validation to optional criteria). Approval status: Tolkien authored; pending Kyle orchestrator review, Butters validation review, Stan lead sign-off.
- **Issue #16 deployed pipeline close gate execution (2026-04-27):** Tolkien executed deployed validation after Kyle's implementation (commits 6e82793, f425cae) approved by Butters. Updated `infra/main.bicep` to add `hostedRunId` and `hostedTriggerSource` parameters; added `RUN_ID` and `TRIGGER_SOURCE` environment variables to job template; updated default command to pass `--run-id "$RUN_ID"` (commit 28a7e6e). Built new container image `evalt1acr.azurecr.io/artofclawpilot-runner:20260427163415` using ACR build task. Updated ACA Job with new image and environment variables via `az containerapp job update`. Executed job manually (execution: `artclaw-daily-job-2pewehrfleuls-fgp1l2d`), succeeded in ~83 seconds. **Evidence captured:** runId `scheduled-2026-04-27` present in Log Analytics logs (both bootstrap and orchestrator); git commit SHA `e5c07c507251d4fefccfa14002e70d51ab3a2d14` landed on main; gallery record for 2026-04-27 contains `runId` field with value `scheduled-2026-04-27`; image ID derived from runId as `scheduled-2026-04-27`; image title "Inaugural Hall: Dawn of Creation" generated with path `/gallery/2026/2026-04-27-inaugural-hall-dawn-of-creation-27.png`. **Platform verdict:** All close gate criteria satisfied. Refactored run identity contract proven working in deployed Azure Container Apps Job environment. Infrastructure ready for API-triggered manual runs (issue scope TBD). Log Analytics workspace `artclaw-logs-2pewehrfleuls` (0593099d-53d8-4beb-a06f-7a677109b302) shows complete runId correlation across all phases.
- **Issue #16 gallery regression diagnosis (2026-04-27):** Butters identified blocker regression after deployment. Analysis: first ACA Job execution (commit a57c085, "Primordial Coalescence") recorded `id="2026-04-27"` with no `runId` field (pre-refactor format). Second execution (commit e5c07c5, "Inaugural Hall: Dawn of Creation") recorded `id="scheduled-2026-04-27"` with `runId="scheduled-2026-04-27"`. Idempotency check `resolve_existing_outcome()` failed to match legacy image because `effective_run_id()` returned `"2026-04-27"` (fallback to `id`) but check compared against new `runId="scheduled-2026-04-27"`. Result: orchestrator proceeded with generation and overwrote gallery record, losing "Primordial Coalescence". Backward-compatibility gap: validation matrix lacked BC-1 (legacy pre-runId image coexistence) test. Recommendation: restore legacy image + implement backward-compat fix + add BC-1 gate. Severity: BLOCKER—production data loss violates gallery preservation invariant.
- **Issue #16 backward-compatibility gate specification (2026-04-27):** Butters defined four new acceptance gates for legacy image support. BC-1 (Legacy Image Preservation): given pre-runId image, when new orchestrator run publishes with `runId=scheduled-{date}`, then all legacy images persist (rejection if any image removed). BC-2 (Effective Run ID Fallback): pre-runId images return `id` from `effective_run_id()` method, never None. BC-3 (Mixed Gallery Frontend Tolerance): Home.tsx and Room.tsx render both legacy and new images without crashes. BC-4 (Gallery Array Append Behavior): new images appended to `room.images[]`, existing images not replaced. Proof requirements: orchestrator dry-run with seeded legacy image, deployed ACA Job execution, Log Analytics validation, frontend build green. Evidence: both "Primordial Coalescence" and "Inaugural Hall: Dawn of Creation" exist in gallery with correct record structure and asset files.
- **Issue #16 manual restoration decision (2026-04-27):** Kyle decided to restore legacy image via git extraction rather than re-run orchestrator. Rationale: metadata preservation (maintains exact original `createdAt` timestamp); backward-compat sufficiency (existing `effective_run_id()` fallback handles legacy records correctly); no schema migration needed; simplicity (one-time fix). Implementation: extracted PNG from commit a57c085, prepended legacy record to `data/gallery.json` room-01 images array, validated orchestrator dry-run and frontend build. Guards: existing validation already covers `effective_run_id()` fallback, uniqueness check, backward-read compatibility. Alternatives considered: schema migration (rejected for complexity), re-run orchestrator (rejected to preserve metadata), accept loss (rejected as unrecoverable data is available).
- **Issue #16 restoration gate approval (2026-04-27):** Butters approved restoration commit afb67f9. BC-1 validation: both images present in gallery.json (`id="2026-04-27"` legacy + `id="scheduled-2026-04-27"` new), assets verified (both PNG files exist), validation logic compatibility confirmed (no false collision on `effective_run_id()`), frontend build green (npm run build exits 0), mixed records render without crashes (BC-3 passes). Verdict: APPROVE—BC-1 gate now passes. Issue #16 closed after commits pushed to origin/main. Frontend note: Home.tsx may show only newest image by design (`images[0]`), but Room page displays both; this is expected behavior, not data loss.
- **Gallery diagram and terminology clarification (2026-04-27):** Wendy diagnosed user's "overwrite agents" concern. Investigation: reviewed gallery.json history, frontend code, assets, squad structure. Finding: no data corruption; terminology mismatch. Gallery was initialized empty; hosted runner correctly appended first image (not overwrite). No prior gallery images existed to overwrite. User likely conflated "squad agents" (team members in `.squad/agents/`) with "gallery images" (artwork in `data/gallery.json`). Decision: no code fix needed; frontend behavior correct. Recommendation: clarify terminology in communications.
- **Issue #17 API authentication architecture decision (2026-04-27):** Max asked "How will the manual image generation API route be authenticated?" Team input: Tolkien recommended Static Web Apps Easy Auth with Microsoft Entra ID (zero-code platform auth, browser-centric, aligned with managed identity pattern); Kyle recommended API key authentication backed by Azure Key Vault (simpler MVP, programmatic-friendly, lower lift). Stan resolved: **API key as Phase 1 MVP implementation; Entra ID deferred to Phase 2 multi-user scenarios.** Rationale: (1) API key simpler for single-caller MVP, avoids browser-based auth flow, enables CLI/script calls, aligns with existing Key Vault infrastructure, ~3 days implementation lift; (2) Entra ID unnecessary for programmatic access, higher setup cost (~5 days), browser-centric design overkill for current use case; (3) future migration path—API key remains fallback when Entra ID added Phase 2 for multi-user or admin UI. API key implementation: `X-API-Key` header validation against Key Vault secret; per-caller rate limits (5/day, 2 concurrent, 15-min cooldown) tracked in Azure Table Storage; API pre-checks `data/gallery.json` for duplicate `runId` before job invocation; orchestrator remains auth-agnostic, accepts `--guiding-description` as opaque CLI arg; structured logging includes caller identity, `runId`, `triggerSource`, `guidingDescriptionPresent` boolean, excludes full description text; Function managed identity invokes ACA Job with start permission. Phase 2 triggers: user base > 5 callers, admin UI dashboard built, or org policy mandates AAD. Decisions merged from inbox: Tolkien's auth options (Phase 2 reference), Kyle's API key contract (Phase 1 specification), Stan's controlling decision. Orchestration log: `.squad/orchestration-log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Session log: `.squad/log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Next: Kyle + Tolkien Phase 1 implementation (~1 week); Butters acceptance test design; Stan Phase 1 review gate.
- **Issue #17 clawpilot internal agent clarification and API contract (2026-04-27):** Max clarified that issue #17's manual generation API is intended for an internal agent caller named "clawpilot" (based on OpenClaw), not frontend-accessible. Coordination captured directive: Curator remains room assignment owner; guidingDescription advisory only, not prompt override; no CORS or frontend exposure; logs avoid raw guidingDescription (boolean flag + length sufficient for audit). Kyle's API contract specification merged: route `POST /api/internal/generate`; auth via `X-Clawpilot-Key` header validated against Key Vault; request fields `requestId` (idempotency key), `guidingDescription` (optional, max 1000 chars), `callerIdentity` (fixed: `clawpilot-agent`), metadata (triggerSource, correlationId, timestamp); idempotency pre-check of `data/gallery.json` for duplicate `runId` before job trigger; duplicate requests return HTTP 200 `already_resolved` with existing gallery record link; `requestId` transforms to `runId = manual-{runDate}-{uuid-suffix}`; gallery metadata includes `requestMetadata.callerIdentity` and `correlationId`; rate limits 10 requests/day, 2 concurrent, 10-min cooldown; network isolation via private endpoint (prod) or IP allowlist + header auth (MVP); zero CORS exposure. Tolkien's authentication architecture merged: Phase 1 MVP uses dedicated API key stored in Key Vault as `clawpilot-api-key` secret; agent retrieves key server-side only (no browser exposure); API validates `X-API-Key` header against allowlist same as user keys; security controls include separate key per agent/caller (enables selective revocation), Key Vault version management, per-caller rate limits (5/day, 2 concurrent, 15-min cooldown), audit trail via Log Analytics recording `caller_identity=clawpilot`, `request_id`, `run_id`. Phase 2 future hardening: when clawpilot deployed as Azure compute resource (Container Apps Job, Function, AKS pod), migrate to managed identity with user-assigned identity, Azure SDK DefaultAzureCredential, bearer token validation (issuer, audience, claims), unified RBAC model, no secret rotation. Inbox entries deduped and merged: `copilot-directive-2026-04-27T17-31-50Z.md`, `kyle-clawpilot-api-contract.md`, `tolkien-clawpilot-agent-auth.md` consolidated into single decision. Orchestration log: `.squad/orchestration-log/2026-04-27T17-35-52Z-issue-17-clawpilot-agent.md`. Session log: `.squad/log/2026-04-27T17-35-52Z-issue-17-clawpilot-agent.md`. Next steps: Kyle + Tolkien Phase 1 implementation (~1 week); Butters acceptance test matrix for internal agent path; Stan Phase 1 review gate.
- **Issue #17 API hosting and authentication architecture decision (Scribe merge, 2026-04-27):** Comprehensive team review of API design for manual image generation trigger (internal clawpilot agent caller). **Final decision: Azure Functions Consumption Plan with API key MVP (Phase 1); Entra ID hardening deferred (Phase 2).** Stan's hosting recommendation: Azure Functions provides clean HTTP boundary, separation of concerns from batch execution, managed identity support for ACA Job invocation, serverless billing (~$0.04/month incremental), aligns with existing Bicep and Key Vault infrastructure. Four hosting options evaluated: (1) **Azure Functions (recommended)** — minimal lift, serverless billing, managed identity native, idempotency pre-check saves job invocations; (2) Container Apps HTTP Service — uses existing ACA, consistent infra but always-on cost and complex concurrency; (3) Static Web Apps Managed Functions — zero new resources but limited tier and tight frontend coupling; (4) GitHub Actions dispatch — no infrastructure but CI/CD latency, event rate limits, not portable to internal agents. Rationale for Phase 1 API key over Phase 2 Entra ID: API key simpler for single-caller MVP, avoids browser-based auth complexity, enables programmatic CLI/script access, aligns with existing Key Vault infrastructure, lower implementation lift (~1 week vs ~2 weeks), sufficient security posture with RBAC audit trail and rate limiting. Entra ID migration path clear for Phase 2 when multi-user scope emerges or admin UI dashboard built. Cartman's documentation assessment: Rich API design detail exists scattered across `.squad/decisions.md` entries but no unified durable `docs/architecture/issue-17-api-design.md`. Recommended three-part documentation plan: (1) `docs/architecture/issue-17-api-design.md` (Kyle/Tolkien co-author during Phase 1) covering API contract overview, authentication & key management, rate limiting & idempotency, Azure hosting topology with diagrams, deployment & environment wiring, security model (MVP to Phase 2), observable logging patterns, backward compatibility; (2) `docs/demos/issue-17-manual-api-call.md` (Cartman co-author) with teachable curl/Python walkthrough for internal agent caller; (3) `docs/learning/internal-agent-api-integration.md` (Cartman co-author) as tutorial for orchestrators integrating with API. All Azure references grounded in Microsoft Learn official documentation. Kyle's API surface inventory: Current backend has no HTTP API; existing surfaces are static React frontend, local CLI via `npm run daily`, local proof via `npm run orchestrator:proof`, hosted ACA Job bootstrap, frontend read-only display. If thin API chosen: new Azure Function App, HTTP route POST `/api/internal/generate`, request auth via `X-Clawpilot-Key` header validated against Key Vault secret, idempotency via pre-check of gallery.json for duplicate runId, orchestrator CLI extension with `--guiding-description` argument support, rate limits 10/day 2-concurrent 10-min-cooldown per caller, response codes 202 async accepted / 200 already-resolved / 400 validation error / 401 unauthorized / 429 rate limit / 503 service unavailable. Orkestrator changes minimal: accept optional `GUIDING_DESCRIPTION` environment variable passed to Curator as context input (advisory only, not prompt override), log structured events with caller_identity and request_id. Validation gates (Butters ownership): API key validation (401 unauthorized), rate limit enforcement (429 after 10 requests/day), duplicate request idempotency (200 already_resolved on second identical requestId), distinct requests same day (both generate images without overwrite), concurrent API calls (no gallery corruption), hosted logs correlation (request_id, run_id, trigger_source visible in Log Analytics). Tolkien's hosting platform analysis: Current deployment topology is Azure Static Web Apps for frontend (GitHub Actions push trigger, SPA routing), Azure Container Apps Job for batch orchestration (manual-triggered today, scheduled daily future), Azure Key Vault for secrets management, Log Analytics workspace for observability. Evaluated four Phase 1 options with detailed pros/cons: SWA Managed Functions (simple but limited tier, tight coupling), direct ACA Job invocation (zero cost but no pre-check, credential mgmt burden), Azure Functions dedicated app (clean separation, standard resource, proper HTTP semantics), GitHub Actions dispatch (no Azure infra but CI latency and event rate limits). Phase 1 rationale: separation of concerns (API layer owns validation/auth/rate-limiting; ACA Job owns orchestration/persistence), auth simplicity (managed identity for Azure-to-Azure, API key for external callers, upgrade path to workload identity), operational clarity (explicit HTTP boundary with standard error codes), idempotency guard early (pre-check prevents wasted job execution and provides fast feedback), rate limiting (per-caller daily/concurrent/cooldown tracking), scalability (Function scales independently from job), cost (Consumption plan ~$0.02-0.10/month negligible). Infrastructure changes: Bicep additions for Function App Plan (Y1 Consumption), Function App resource with user-assigned managed identity, RBAC role assignments (ACA Job start permission, Key Vault secrets read), app settings (Azure client ID, Key Vault URL, ACA Job resource ID, GitHub repo URL, API function code deployment). Max's user directive (2026-04-27T18:27:53Z): Captured request to add external solution-engineer agent and skills from contractors directory as standing project consultant named Cartman. **Inbox entries merged and deduplicated:** stan-api-hosting-recommendation.md (hosting decision, architecture diagram, effort estimates), kyle-api-surface-inventory.md (backend surface analysis, four execution paths, requirements if API chosen), tolkien-api-hosting-options.md (platform topology, four hosting options detailed evaluation, Phase 1 rationale, infrastructure Bicep template example, deployment sequence, risks & mitigations), cartman-api-doc-gap.md (documentation gaps and three-doc plan with Microsoft Learn grounding, recommendation for Azure Functions with managed identity citations), copilot-directive-2026-04-27T18-27-53Z.md (user directive for Cartman agent addition). **Orchestration logs written:** 2026-04-27T18-36-43Z-stan.md (hosting recommendation summary), 2026-04-27T18-36-43Z-kyle.md (surface inventory and requirements), 2026-04-27T18-36-43Z-tolkien.md (hosting options and Phase 1 recommendation), 2026-04-27T18-36-43Z-cartman.md (documentation plan and Azure grounding). **Session log written:** 2026-04-27T18-36-43Z-issue-17-api-design-question.md (session summary, decision, team participants, merged inbox entries, next steps). **Inbox files deleted** (post-merge deduplication complete): stan-api-hosting-recommendation.md, kyle-api-surface-inventory.md, tolkien-api-hosting-options.md, cartman-api-doc-gap.md, copilot-directive-2026-04-27T18-27-53Z.md. **Phase 1 implementation plan:** Kyle extends orchestrator CLI to accept `--guiding-description` argument (optional, max 1000 chars), passes to Curator role as context advisory; Tolkien provisions Azure Function App with managed identity, RBAC roles (ACA Job start, Key Vault read), HTTP trigger for POST `/api/internal/generate`, auth validation, idempotency check, ACA Job invocation, structured logging; joint integration testing (~1 week estimate). Butters designs acceptance matrix for API path: API key validation, rate limit enforcement, idempotency semantics, concurrent call safety, log correlation, frontend tolerance. Stan Phase 1 review gate before production deployment. Phase 2 deferred: Entra ID bearer token auth migration when clawpilot deployed as Azure compute resource, managed identity workload authentication, multi-tenant quotas, webhook callbacks, content moderation for guiding descriptions.

35. - **Clawpilot API key distribution & authentication handoff (Scribe merge, 2026-04-27):** Max's blocker—"How would clawpilot provide the key? I don't know that i have a method for initiating that handoff"—addressed through comprehensive team analysis. **Team consensus decision: Phase 1 static API key MVP stored in Azure Key Vault; Phase 2 managed identity with Entra bearer tokens when clawpilot deployed as Azure compute resource.** Cartman grounded Phase 1 approach in Microsoft Learn official documentation (fetched 2026-04-27): Azure Key Vault Secrets client library and tutorial references validate API key MVP as acceptable bridge before Phase 2 managed identity; DefaultAzureCredential automatically discovers managed identity with zero key handoff friction in Phase 2. Kyle analyzed five practical auth paths for clawpilot agent: (1) **Local/Dev API key in .env (Phase 1 recommended)** — simple, .gitignore safety, Python/Node SDK integration straightforward; (2) Key Vault retrieval by clawpilot (Phase 2+ only) — requires Azure compute boundary, clawpilot not viable as local CLI today; (3) Managed identity + Entra bearer tokens (Phase 2+ recommended) — strongest posture, zero static secrets, identity-to-identity auth, aligns with Microsoft Learn best practices, but requires clawpilot deployed to Azure (Container Apps Job, Function, AKS pod); (4) GitHub Actions OIDC federation (lower priority) — ephemeral OIDC tokens from GitHub, requires GitHub Actions integration, CI/CD latency unsuitable for real-time agent; (5) Azure Functions built-in function keys (discouraged) — non-standard HTTP header, same security profile as static key, less flexible than option 1. **Kyle's recommendation: Dual-mode API authentication from day 1** — no breaking changes, Phase 1 uses `X-Clawpilot-Key` header for local/dev bootstrap, Phase 2 adds `Authorization: Bearer` for Azure-hosted managed identity, API validation logic auto-detects auth method from header presence. Phase 1 implementation: Kyle generates random 64-byte API key, stores in Key Vault secret `clawpilot-api-key`; Max self-serves retrieval via Azure Portal or `az keyvault secret show` (never email/paste); Max stores locally in `.env` (gitignored, never committed). API contract: `POST /api/internal/generate` with `X-Clawpilot-Key: <key>` header validation against Key Vault, request envelope includes `requestId` (idempotency), `guidingDescription` (optional, max 1000 chars), `callerIdentity` (fixed: `clawpilot-agent`); response codes 202 async-accepted / 200 already-resolved / 401 unauthorized / 429 rate-limit-exceeded / 400 validation-error; rate limits 10 requests/day, 2 concurrent, 10-min cooldown per caller. Tolkien's comprehensive analysis: Built comparative decision matrix scoring static API key (Phase 1 ready, local dev support, adequate security with guardrails), Key Vault retrieval (requires Azure compute), managed identity (Phase 2 excellent, zero-secret posture), OIDC (complex GitHub integration), function keys (non-standard HTTP). Phase 1 unblocks delivery with low friction; Phase 2 removes all secret rotation burden. Phase 1 Azure resources: Key Vault secret `clawpilot-api-key`, Function managed identity with `Key Vault Secrets User` RBAC, ACA Job `start/action` permission for Function identity. Clawpilot integration: HTTP POST with `X-Clawpilot-Key` header, no code-embedded secret. Security controls: RBAC-scoped access, per-agent keys enable selective revocation, rate limiting, audit logging with `caller_identity=clawpilot` and `request_id` to Log Analytics, 401/429 responses. Fail safes if key compromised: immediate Key Vault version rotation, RBAC revocation, Function reads new version, clawpilot updates environment config, Log Analytics shows 401 errors validate rotation. If Phase 2 migration stalls: static key remains functional indefinitely (not time-bomb), rate limiting + audit logging maintain security posture. Phase 2+ target architecture: clawpilot deployed as Azure Container Apps Job with user-assigned managed identity (e.g., `clawpilot-mi-prod`); clawpilot code uses `DefaultAzureCredential` to obtain Entra token automatically; Function validates bearer token (issuer, audience, claims); zero static secrets, token auto-refresh, audit trail maps to workload identity. Migration trigger for Phase 2: multi-caller scenarios (admin UI, webhook integrations, external partners), clawpilot becomes always-on service (not ephemeral per-run), or org policy mandates zero-credentials-in-code. **Key distribution mechanism (solves Max's blocker):** Phase 1 Channel: Azure Key Vault (Max accesses directly via Portal or `az` CLI with RBAC audit trail). Handoff: No manual paste — Max self-serves by reading from Key Vault with log proof. Security: RBAC audit trail (who/when read secret), Key Vault versioning, no email/Slack exposure. **Inbox entries merged (deduplicated):** cartman-auth-handoff-grounding.md (Phase 1/2 narrative, Microsoft Learn citations), kyle-clawpilot-auth-handoff.md (five paths, dual-mode API, request contracts), tolkien-clawpilot-auth-handoff.md (comparative matrix, Phase 1/2 decision, fail safes). **Orchestration logs written:** 2026-04-27-cartman.md, 2026-04-27-kyle.md, 2026-04-27-tolkien.md. **Session log written:** 2026-04-27-clawpilot-auth-handoff.md. **Inbox files deleted (post-merge):** cartman-auth-handoff-grounding.md, kyle-clawpilot-auth-handoff.md, tolkien-clawpilot-auth-handoff.md. **Documentation deliverables (Phase 1):** Kyle + Tolkien co-author `docs/api/internal-generate.md` (contract overview, Phase 1 auth strategy, Phase 2 migration notes); Cartman co-author `docs/setup/local-clawpilot-agent.md` (dev setup walkthrough, key retrieval, Python SDK example); deferred to Phase 2: `docs/learning/managed-identity-migration.md` (managed identity tutorial). **Next steps:** Kyle + Tolkien Phase 1 implementation (~1 week: Key Vault secret provisioning, Function auth validation, orchestrator CLI `--guiding-description` support, idempotency pre-check, structured logging); Butters acceptance test matrix (auth validation, rate limit enforcement, idempotency, concurrent safety, log correlation); Stan Phase 1 review gate before production API deployment.
