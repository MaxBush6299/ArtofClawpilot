# History - Stan

## Seed Context

- Requested by Max Bush.
- Project: Art of Clawpilot, an AI-powered virtual art gallery that adds one new piece each day.
- Stack: React + Vite frontend, Python orchestration/runtime, Azure Container Apps Jobs, Azure Key Vault, Microsoft Foundry, Azure Static Web Apps.
- Current issue focus: #2, the hosted daily-run architecture and execution contract.

## Learnings

- Team initialized on 2026-04-24 with South Park cast names.
- 2026-04-24 backlog review: issue #2 is the gating contract; recommended flow is architecture first, then platform/auth foundation, then runner implementation, then validation/observability/docs/tests.
- 2026-04-24 backlog review update: final order is #2 (Stan), then #3/#4/#5/#12, then #6/#7/#8/#9/#10/#11/#13, then #14/#15; dependency chain is #2 -> #3/#4/#5/#12 -> #7/#8/#9/#11 -> #6 -> #10/#13 -> #14/#15.
- 2026-04-24 issue #2 draft landed at `docs/architecture/hosted-daily-run.md`; it recommends one Python orchestrator in Azure Container Apps Job, with Curator/Critic/Artist as in-process role steps and a day-scoped idempotency contract.
- 2026-04-24 issue #2 draft recommends GitHub App-based git push to `main` for the publish transaction and a user-assigned managed identity on the job for Key Vault and Foundry auth.
- 2026-04-24 issue #2 kickoff recorded: `docs/architecture/hosted-daily-run.md` is linked from `README.md`; no blocking clarification is needed, and the only open assumption is whether the automation GitHub App can push directly to `main`.
- 2026-04-24 clarification update: Microsoft-specific implementation guidance for the hosted daily run now explicitly cites Microsoft Learn, including UTC cron behavior for Container Apps Jobs and Bicep support for scheduled jobs with user-assigned managed identity.
- 2026-04-24 model split update: hosted reasoning is standardized on the deployed `grok-4-20-reasoning` model, while hosted image generation stays on `MAI-Image-2e`.
- 2026-04-24 clarification closeout: issue #2 no longer has blocking clarification work; the hosted daily-run doc now grounds Microsoft choices in Microsoft Learn and keeps the `grok-4-20-reasoning`/`MAI-Image-2e` split explicit.
- 2026-04-24 issue detail review: the backlog is mostly executable after #2, but #6 should be rewritten to match the Python orchestrator target and #7/#8/#9/#11/#15 should make the Artist handoff explicit as a bounded three-call `grok-4-20-reasoning` flow (analyze -> prompt draft -> prompt review) before the final `MAI-Image-2e` call.
- 2026-04-24 issue detail review closeout: do not spread backlog detail wholesale right now; only refine #6 for the Python orchestrator target plus #7/#8/#9/#11/#15 for explicit Artist I/O, bounded reasoning flow, reviewed prompt handoff, and validation/tests around call bounds and malformed reasoning outputs.
- 2026-04-24 execution sharpening pass: issue titles/bodies for #6/#7/#8/#9/#11/#15 are the concrete execution contract, with #6 as Python orchestrator integration after #7/#8/#9/#11 land.
- 2026-04-24 execution sharpening pass: `docs/architecture/hosted-execution-plan.md` is the concise team sequence doc; the delivery order stays #2 -> #3/#4/#5/#12 -> #7/#8/#9/#11 -> #6 -> #10/#13 -> #14/#15, and no blocker remains.
- 2026-04-24 execution readiness closeout: first execution wave is #3/#4/#5/#12; Tolkien owns foundation, Kyle owns runtime contracts and orchestrator, Butters owns validation/cutover, Wendy owns frontend and data-contract follow-through, and Stan reviews phase exits.
- 2026-04-24 remaining-work review: GitHub issue status is lagging the repo state. Close #8 and #9 immediately after recording approval/landed code, then run a closure pass on #10/#11 against `orchestrator/main.py`, `orchestrator/validation.py`, `orchestrator/integrations/foundry.py`, and `orchestrator/roles/artist.py` before treating #13, #15, and final #14 docs as the cutover sequence.
- 2026-04-24 #13 revision: `scripts/hosted-bootstrap.mjs` must be a first-class observability peer to `orchestrator/main.py`, carrying the same `runDate`, `traceId`, and ACA execution metadata while forwarding `HOSTED_TRACE_ID` into the Python runner.
- 2026-04-24 #13 revision: bootstrap failure logs need structured triage fields (`errorCode`, `exitCode`, `command`, redacted stdout/stderr excerpts) so auth/git/bootstrap failures are queryable in ACA Job + Log Analytics without changing the one-image-or-one-skip runtime contract.
- 2026-04-24 hosted deployment triage: Max deployed all resources but git push failed with "Permission denied... exitCode 128". GHCP investigation confirmed the clone succeeded, MAI generation worked, but the GitHub App likely lacks Contents: Read & Write permissions. The team's architecture already contemplated branch protection blocking the app, but the immediate blocker is repository permissions, not branch rules. The correct path is to update the GitHub App permissions to include Contents: Read & Write, verify the installation accepts the new scope, then rerun the job. No architecture change is needed; the permission surface was an assumption in the original design that was not explicitly documented as a GitHub App setup prerequisite.
- 2026-04-27 team triage synthesis: Stan synthesized findings from Tolkien, Kyle, and Butters. Root cause confirmed as GitHub App permissions boundary. Documentation gap identified: add GitHub App permission prerequisites (Contents: Read & Write for git push to main) to `docs/architecture/hosted-daily-run.md` as setup requirement, not troubleshooting note. Direct push to main remains primary design. Approved recovery path: create disposable `hosted-smoke` branch, fix permissions, redeploy job with `githubBranch=hosted-smoke`, rerun Phase B proof on branch. After smoke proof passes, production cutover resumes with intended direct-push-to-main design.
- 2026-04-27 frontend guide correction: `runDate` is a `YYYY-MM-DD` day key validated by `orchestrator/validation.py`, not a full ISO timestamp.
- 2026-04-27 frontend behavior note: `src/pages/Home.tsx` uses `r.images?.[0]` for room covers while the orchestrator appends new images, so hosted smoke should treat room-count growth as the reliable Home-page signal unless the room was previously empty.
- 2026-04-27 rendering detail: `src/pages/Room.tsx` conditionally renders `img.criticism` with a truthy check rather than optional chaining; missing criticism simply omits the blockquote.
- **2026-04-27 cross-team note:** Revised Wendy's `docs/architecture/frontend-hosted-update-guide.md` after Butters rejection (five inaccuracies). Corrections applied: (1) `runDate` as day key not timestamp; (2) `Room.tsx` truthy check not optional chaining; (3) removed JSON import schema claim; (4) fixed Home cover assumption; (5) clarified contract boundaries. Butters re-reviewed and approved after all corrections verified. Orchestration logs recorded for Wendy/Butters/Stan. Session log and decisions merged. Frontend approval scope: smoke practicality accuracy; broader hosted gates remain in `hosted-smoke-checklist.md`.
- **2026-04-27 multi-run invariant analysis:** Max proposed API-triggered manual runs allowing multiple images per day. Analyzed shift from day-scoped idempotency (`runDate` uniqueness) to run-scoped idempotency (`runId` uniqueness). Core change: `runId` becomes primary key, `runDate` becomes grouping field. Scheduled runs use `runId=scheduled-{runDate}` preserving one-per-day contract; manual runs use `runId=manual-{runDate}-{uuid}` allowing multiple. Document in `.squad/decisions/inbox/stan-multi-run-invariant.md` covers schema changes, idempotency gate updates, asset path uniqueness, rate limits, and rollout sequence. Key files affected: `orchestrator/contracts.py`, `orchestrator/validation.py`, `orchestrator/main.py`, `scripts/hosted-bootstrap.mjs`, plus new `/api/generate` endpoint. Frontend impact minimal (already handles multiple images). Decision gates: confirm acceptance of multiple-images-per-day invariant change, define rate limits, approve rollout plan.
- **2026-04-27 API rollout lift analysis:** Analyzed two implementation paths for API integration: (1) Sequential—refactor idempotency first, then API later; (2) Parallel—design and build API alongside idempotency refactor. Recommendation: **Path 1 Sequential**. Rationale: lower total risk (~15–22 days elapsed, two proof cycles), isolates failures, allows scheduler promotion independent of API readiness, clearer team parallelization, incremental confidence signal. Path 2 parallel work has higher integration risk (both or nothing cutover) and larger test matrix (15+ combined scenarios). Work packages: Phase 1a/b/c (Kyle + Tolkien = 6–9 days) → Phase 1d smoke proof (Butters = 1–2 days) → Phase 2a/b/c (API layer = 6–7 days) → Phase 2d API smoke proof (3–4 days). Validation gates: Phase 1d must prove scheduled run idempotency and asset uniqueness; Phase 2d must prove concurrent API safety, rate limits, and multi-run persistence. Key decision ahead: confirm Rate limits (max manual runs/day), Auth boundary (admin-only or user auth), Room assignment (Curator logic or API-specified). Document in `.squad/decisions/inbox/stan-api-rollout-lift.md`.
- **2026-04-27 session merge:** Stan's multi-run analysis documents merged into `.squad/decisions.md` (2026-04-27 section) from inbox. Scribe recorded orchestration logs (.squad/orchestration-log/2026-04-27T15-55-45Z-stan-api-rollout-lift.md). Inbox files deleted post-merge. Session log recorded (.squad/log/2026-04-27T15-55-45Z-api-rollout-sizing.md). Cross-team updates appended to Stan, Kyle, Butters history files.
- **2026-04-27 issue #16 architecture alignment:** Stan refactored four architecture docs to support run-identity-scoped idempotency (primary key shift from `runDate` to `runId`). Changes propagated through `hosted-daily-run.md` (problem statement, invariant section, execution contract, log fields), `hosted-execution-plan.md` (validation notes), `hosted-validation-gates.md` (release gates, validation categories, dry-run coverage, promotion bar), `hosted-smoke-checklist.md` (Phase B idempotent-rerun proof), and `frontend-hosted-update-guide.md` (multi-image-per-runDate tolerance). Terminology shift: "one-outcome-per-day" → "run-identity-scoped idempotency"; "day-scoped" → "run-identity-scoped" where applicable. Key architectural refinements documented: multiple images per `runDate` allowed with distinct `runId` values; skip-closes-day semantics (any skip for `runDate` blocks all further runs for that day); new `already_resolved` and `day_already_closed` orchestrator exit paths; `runId` added to image/skip record structures and bootstrap/orchestrator log correlation fields. All changes conflict-free; no files Kyle/Butters/Tolkien actively modifying were edited. Commit hash: 461e5ed, issue #16 reference included with Co-authored-by trailer.
- **2026-04-27 issue #16 rejection fix:** Butters rejected Kyle's commit 6e82793 because `orchestrator/main.py` created gallery image records with `id=context.run_date` instead of `id=context.run_id`, causing duplicate image_id errors when publishing multiple images on the same day with different runIds. Stan fixed the bug by changing line 375 from `id=context.run_date` to `id=context.run_id`. Also fixed tightly coupled asset path collision issue: added `run_id_suffix` extraction (last 8 chars of runId) and updated asset paths from `{runDate}-{slug}.png` to `{runDate}-{slug}-{runId-suffix}.png` on lines 371-373. Validation proof: ran two same-day dry-runs with different runIds (`scheduled-2099-03-11` and `manual-2099-03-11-abc123`), confirming distinct image IDs and asset paths (`2099-03-11-dry-run-radiance-11.png` vs `2099-03-11-dry-run-radiance-abc123.png`). Both runs completed successfully without collision. Commit hash: f425cae, issue #16 reference included with Co-authored-by trailer. Kyle lockout respected; Stan owned the revision as Lead.
- **2026-04-27 issue #16 closeout:** Issue #16 (run identity refactor) successfully closed. Kyle's implementation (commit 6e82793) was rejected due to blocking defect (image_id still used `context.run_date`). Reviewer lockout applied. Stan fixed the defect (commit f425cae, approved). Tolkien executed deployed validation generating new gallery image "Inaugural Hall: Dawn of Creation" (execution `artclaw-daily-job-2pewehrfleuls-fgp1l2d`, commit `e5c07c507251d4fefccfa14002e70d51ab3a2d14`). All close gates satisfied. Infrastructure now ready for multi-run support. Stan's fix successfully resolved Kyle's blocking defect; code now deployed and working in production.
- **2026-04-27 issue #17 auth decision:** Stan resolved API authentication architecture for manual image generation. Recommended: **API key as first implementation** (Kyle's proposal) over Static Web Apps Easy Auth (Tolkien's proposal). Rationale: API key is simpler for single-user MVP, avoids browser-based auth flow, enables programmatic calls from CLI/scripts, aligns with existing Key Vault infrastructure, lower implementation lift (~3 days vs ~5 days), and sufficient security with RBAC audit trail. Entra ID deferred to Phase 2 for multi-user scenarios or when admin UI requires identity-based permissions. API key lifecycle managed via Key Vault with manual rotation by Max. Rate limits (5/day per caller) enforced at API layer via Azure Table Storage tracking. Authentication contract: `X-API-Key` header validated against Key Vault secret; API pre-checks `runId` uniqueness in gallery before job invocation; orchestrator remains auth-agnostic (accepts `--guiding-description` as opaque CLI arg). Security posture: Key Vault RBAC for secret access, structured logging excludes full description text, API Function uses managed identity for ACA Job invocation, no static secrets in orchestrator code. Auditability: caller identity logged (from key owner metadata), `runId` and `triggerSource` in all logs, per-request correlation IDs. Deferred to Phase 2: Entra ID for multi-user auth, managed identity for Azure service callers, webhook callbacks for job completion, content moderation for guiding descriptions. Decision document: `.squad/decisions/inbox/stan-issue-17-auth-decision.md`.
- **2026-04-27 session merge:** Stan's multi-run analysis documents merged into `.squad/decisions.md` (2026-04-27 section) from inbox. Scribe recorded orchestration logs (.squad/orchestration-log/2026-04-27T15-55-45Z-stan-api-rollout-lift.md). Inbox files deleted post-merge. Session log recorded (.squad/log/2026-04-27T15-55-45Z-api-rollout-sizing.md). Cross-team updates appended to Stan, Kyle, Butters history files.
- **2026-04-27 issue #17 auth decision:** Stan resolved API authentication architecture for manual image generation. Recommended: **API key as first implementation** (Kyle's proposal) over Static Web Apps Easy Auth (Tolkien's proposal). Rationale: API key is simpler for single-user MVP, avoids browser-based auth flow, enables programmatic calls from CLI/scripts, aligns with existing Key Vault infrastructure, lower implementation lift (~3 days vs ~5 days), and sufficient security with RBAC audit trail. Entra ID deferred to Phase 2 for multi-user scenarios or when admin UI requires identity-based permissions. API key lifecycle managed via Key Vault with manual rotation by Max. Rate limits (5/day per caller) enforced at API layer via Azure Table Storage tracking. Authentication contract: `X-API-Key` header validated against Key Vault secret; API pre-checks `runId` uniqueness in gallery before job invocation; orchestrator remains auth-agnostic (accepts `--guiding-description` as opaque CLI arg). Security posture: Key Vault RBAC for secret access, structured logging excludes full description text, API Function uses managed identity for ACA Job invocation, no static secrets in orchestrator code. Auditability: caller identity logged (from key owner metadata), `runId` and `triggerSource` in all logs, per-request correlation IDs. Deferred to Phase 2: Entra ID for multi-user auth, managed identity for Azure service callers, webhook callbacks for job completion, content moderation for guiding descriptions. Decision document: `.squad/decisions/inbox/stan-issue-17-auth-decision.md`.
- **2026-04-27 issue #17 decision closure (Scribe):** Inbox entries merged into `.squad/decisions.md`. Stan's controlling decision recorded: API key Phase 1 MVP adopted; Entra ID Phase 2 deferred. Orchestration log: `.squad/orchestration-log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Session log: `.squad/log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Inbox files deleted (merged from Tolkien, Kyle, and Stan recommendations). Next: Kyle + Tolkien Phase 1 implementation (~1 week); Butters acceptance matrix design; Stan Phase 1 review gate. Approval status: Stan approved, Max/Kyle/Tolkien/Butters pending Phase 1 implementation review.
- **2026-04-27 issue #17 API hosting and auth comprehensive team review (Scribe):** Four agents (Stan, Kyle, Cartman, Tolkien) provided comprehensive input on API hosting and authentication architecture for issue #17 manual generation trigger (internal clawpilot agent). **Final decision: Azure Functions Consumption Plan with API key Phase 1 MVP; Entra ID Phase 2 deferred.** Stan's hosting recommendation evaluated four options; Azure Functions chosen for clean boundary, managed identity support, serverless billing (~$0.04/month), Key Vault alignment. Kyle's API surface inventory confirmed no HTTP API exists today; documented four execution surfaces; defined API requirements if chosen (auth, idempotency, rate limits, orchestrator CLI extension). Tolkien's platform analysis detailed Azure topology evaluation; Phase 1 rationale for Functions (separation of concerns, auth simplicity, operational clarity, idempotency guard, rate limiting, scalability); infrastructure Bicep template pattern provided. Cartman's documentation assessment identified design detail scattered across decisions.md; recommended three-part doc plan (architecture, demo, learning); all Azure claims grounded in Microsoft Learn. Orchestration logs recorded for each agent (4 logs). Session log captured. Inbox entries merged and deduplicated (5 files deleted post-merge). **Cross-team coordination:** Kyle + Tolkien Phase 1 implementation (orchestrator CLI extension + Azure Function provisioning, ~1 week); Butters acceptance matrix design (API key validation, rate limiting, idempotency, concurrency, logs); Stan Phase 1 review gate. Phase 2 deferred until multi-user scope emerges or admin UI dashboard built. Orchestration logs: 2026-04-27T18-36-43Z-{stan|kyle|tolkien|cartman}.md. Session log: 2026-04-27T18-36-43Z-issue-17-api-design-question.md. Merged decisions recorded in .squad/decisions.md (entry 34).
