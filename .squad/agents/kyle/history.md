# History - Kyle

# History - Kyle

## Seed Context

- Requested by Max Bush.
- Project: Art of Clawpilot, an AI-powered virtual art gallery that adds one new piece each day.
- Stack: React + Vite frontend, Python orchestration/runtime, Azure Container Apps Jobs, Azure Key Vault, Microsoft Foundry, Azure Static Web Apps.
- Current issue focus: #2, the hosted daily-run architecture and execution contract.

## Core Context

**Role:** Kyle owns Python orchestration, Foundry integration, and persisted runtime contracts. Responsible for issue #16 run identity refactor implementation and API rollout sizing.

**2026-04-24 Work:** Implemented runtime contract slice in orchestrator/ with typed role contracts, reusable validation, bounded Artist flow (analyze/draft/review), and preflight state loading. Role parsing machine-validates model output. Skip records carry nested error metadata. Implemented orchestrator/main.py with JSONL phase diagnostics, runDate/traceId context for ACA observability. Created npm run orchestrator:proof for fixture-driven local validation (publish/skip/malformed/budget/no-op scenarios). Live proof against Azure reached MAI but failed on HTTP 500 endpoint config. GitHub App permissions issue confirmed blocking git push (exitCode 128). Architecture review confirmed direct push to main preserves idempotency invariant.

**2026-04-27 (Pre-Session):** Designed multi-run idempotency contract using runId as primary key (format {runDate}-{source}-{seq}). Implemented CLI --run-id, bootstrap RUN_ID injection, validation checks, backward compatibility via effective_run_id() fallback. Sized two API rollout options: orchestrator-only (2-3 days), full API integration (1-2 weeks). Recommended sequence: prove orchestrator contract first with manual CLI, add API layer incrementally. Direct ACA Job invocation chosen (not enqueue). Lock-free concurrency via runId atomicity. API pre-checks gallery before invocation. Implemented --run-id support in contracts.py, validation.py, main.py; dry-run proof passed. Diagnosed gallery regression: second execution overwrote first (both same runDate, distinct runId mismatch). Manual restoration recovered legacy image via git extraction preserving metadata. Issue #16 code deployed successfully after Stan's fix.

## Learnings (2026-04-27 Session)

- **2026-04-27 team triage consensus:** Kyle's architecture review confirmed direct push to main preserves design intent and one-commit-per-day invariant. PR-based flow would split "complete run" from "published outcome" and add idempotency complexity, violating documented contract. Team decision: fix GitHub App permissions (Option A). After permissions fix and disposable `hosted-smoke` branch proof with idempotent rerun validation, production cutover resumes with intended direct-push-to-main design.
- **2026-04-27 multi-run idempotency design:** Max asked about supporting manual API-triggered runs in addition to scheduled daily runs without duplicates. Kyle designed backend contract introducing `runId` as primary idempotency key (format `{runDate}-{source}-{seq}`), keeping `runDate` as day-level granularity. Key decisions: each run gets unique `runId`, each image gets unique `imageId` with collision suffix, skip records indexed by `runId`, file naming supports multiple images per day, API route queues new runs with distinct `runId`, same `runId` rerun = no-op, git commits remain atomic one-per-publish. Three-phase migration: (1) add optional `runId` support backward-compatible, (2) implement API route with auth, (3) enforce uniqueness. Proposal written to `.squad/decisions/inbox/kyle-multi-run-idempotency.md` covering idempotency keys, data shape, file naming, CLI/API inputs, logs, retry behavior, and coexistence of scheduled + manual runs.
- **2026-04-27 API rollout lift sizing:** Max asked about implementation lift for deploying multi-run support. Kyle sized two options: (1) orchestrator-only update (2-3 days: add `runId` contract, update idempotency checks, validate multi-run path, no infra changes), (2) full API integration (1-2 weeks: Option 1 plus Azure Function App, auth layer, request validation, ACA Job invocation, execution tracking, rate limiting). Key architectural decisions: direct ACA Job invocation (not enqueue-first) because job has built-in retry and queue adds little value; lock-free concurrency (orchestrator resolves via `runId` rather than advisory lock); API checks gallery state before invocation to prevent wasted job executions. Concrete touchpoints identified: `orchestrator/contracts.py` + `validation.py` + `main.py` for Option 1; new `api/trigger-run/*` Functions code + Bicep infra modules for Option 2. Recommended sequence: prove Option 1 (orchestrator contract) with manual CLI triggers first, then add API layer behind feature flag once validation gates pass. Cost estimate: ~$0.04/month incremental for API layer (negligible). Sizing doc written to `workspace/kyle-api-rollout-lift-sizing.md` covering code areas, infrastructure changes, duplicate-trigger prevention, monitoring, and three-phase rollout timeline.
- **2026-04-27 session merge:** Multi-run idempotency and API rollout sizing documents merged into `.squad/decisions.md` (2026-04-27 section) from inbox. Scribe recorded orchestration log (.squad/orchestration-log/2026-04-27T15-55-45Z-kyle-api-rollout-lift.md). Inbox files deleted post-merge. Session log and cross-team updates recorded.
- **2026-04-27 issue #16 implementation:** Refactored orchestrator/data contract to use durable run identity (`runId`) instead of `runDate` alone for idempotency. Added `runId` field to `RuntimeConfig`, `GalleryImageRecord`, `SkipRecord`, and `RunContext` contracts in `orchestrator/contracts.py`. Updated `resolve_existing_outcome()` in `orchestrator/validation.py` to check `runId` exact match for skip/publish outcomes, enabling distinct same-day runs while preserving duplicate rejection. Added CLI/env input `--run-id` with default `scheduled-{runDate}` format to maintain scheduled daily behavior. Updated `hosted-bootstrap.mjs` to pass `RUN_ID` env var with default `scheduled-{runDate}`. Changed validation from `runDate` uniqueness to `runId` uniqueness: `validate_gallery_state()` now tracks `seen_run_ids` for images and skip records. Removed `ensure_run_date_available()`, added `ensure_run_id_available()`. Updated asset naming keeps `runDate` in filename; collision handling can extend with `runId` suffix in future. Backward compatibility: old images without `runId` use `effective_run_id()` falling back to `id`. Skip records backward-read via `from_dict()` defaulting missing `runId` to `id`. CLI defaults preserve scheduled behavior; manual runs require explicit `--run-id` override. Dry-run proof passed with custom `runId=test-run-2099-01-01-abc123`. Frontend build green (no code changes needed; UI tolerates multiple images per `runDate`). Logs include `runId` context. Deployed pipeline validation deferred to Tolkien per issue #16 acceptance gate.
- **2026-04-27 gallery overwrite diagnosis:** Max reported seeing "new image in the gallery, but it seems to have overwritten the existing agents." Investigation revealed no overwrite occurred. Evidence: (1) Git history shows `data/gallery.json` had only three commits (fc2756e initial scaffold, f827d6e GitHub App bootstrap, e5c07c5 hosted runner 2026-04-27), all with `room-01.images` empty until e5c07c5 which added the first published image. (2) Commit e5c07c5 diff shows append-only behavior: `target_room.images.append(outcome.image_record)` in `orchestrator/main.py:414`. (3) Squad agent charter files in `.squad/agents/*` unchanged by the hosted run (diff e5c07c5^..e5c07c5 showed zero `.squad/` changes). (4) No prior images existed; `public/gallery/` contained only `.gitkeep` and the new `2026-04-27-inaugural-hall-dawn-of-creation-27.png`. Root cause hypothesis: Max may have confused (A) Squad project agents (Butters, Kyle, Stan, etc. in `.squad/agents/`) with (B) gallery image content/metadata, or (C) expected pre-seeded sample images that were never present. No fix needed—orchestrator write path is correct and append-only as designed. Frontend Home.tsx renders `r.images?.[0]` as the room cover, which correctly shows the first (and only) image. Recommendation: clarify with Max whether concern is about Squad agents, gallery images, or frontend display behavior.
- **2026-04-27 issue #16 closeout:** Issue #16 (run identity refactor) successfully closed. Kyle's implementation (commit 6e82793) was rejected due to blocking defect (image_id still used `context.run_date`). Reviewer lockout applied. Stan fixed the defect (commit f425cae, approved). Tolkien executed deployed validation generating new gallery image "Inaugural Hall: Dawn of Creation" (execution `artclaw-daily-job-2pewehrfleuls-fgp1l2d`, commit `e5c07c507251d4fefccfa14002e70d51ab3a2d14`). All close gates satisfied. Infrastructure now ready for multi-run support. Kyle's code successfully deployed end-to-end with refactored contract working correctly in production.
- **2026-04-27 issue #16 regression recovery:** User reported gallery image overwrite regression. Investigation revealed hosted runner commit e5c07c5 (2026-04-27, "Dawn of Creation") replaced earlier commit a57c085 (2026-04-27, "Primordial Coalescence") because pre-runId implementation used only `runDate` for idempotency. Root cause: both images shared same `runDate` but lacked distinct `runId` values before refactor. Recovery approach: manually restored legacy image record and PNG asset from commit a57c085 into current main, placing it alongside the new image in `data/gallery.json`. Legacy record uses `id: "2026-04-27"` with no `runId` field; `effective_run_id()` backward-compatibility fallback preserves validation. New record uses `id: "scheduled-2026-04-27"` and `runId: "scheduled-2026-04-27"`. Both coexist with distinct effective runId values. Validation evidence: orchestrator dry-run proof (2026-04-29, test-legacy-check) passed preflight validation with two 2026-04-27 images; frontend `npm run build` green. Key lesson: `effective_run_id()` fallback in `orchestrator/contracts.py` (line 109-110) and validation loop in `orchestrator/validation.py` (line 102-104) correctly handle pre-runId legacy records without schema migration. No guard code needed—existing backward-compatibility design sufficient. Commit afb67f9 references issue #16, includes Co-authored-by trailer. Manual git restoration preserved exact original metadata (createdAt timestamp) versus re-running orchestrator which would create new timestamp.
- **2026-04-27 issue #16 recovery closeout (Scribe):** Restoration commit afb67f9 approved by Butters. Manual git extraction preserved legacy image PNG and metadata; prepended to gallery.json alongside new image. BC-1 gate now passes. Both "Primordial Coalescence" and "Inaugural Hall: Dawn of Creation" coexist. Issue #16 re-closed after commits pushed to origin/main. Squad documentation fully logged and archived.
- **2026-04-27 issue #17 auth contract recommendation:** Max requested auth contract design for manual API-triggered generation (issue #17). Kyle recommended API key-based auth (MVP Tier 1) with Azure Functions + Key Vault integration. Key design decisions: (1) API key validation at function layer with per-caller rate limiting (5 requests/day, 2 concurrent, 15-min cooldown), (2) idempotency binding via `requestId` checked against GitHub `data/gallery.json` state before job trigger (prevents duplicate job waste), (3) `guidingDescription` treated as opaque Curator context (not raw prompt injection), (4) structured logging with caller identity and request metadata but excluding full description text (protect user creative content), (5) request validation rejects oversized/malformed payloads (max 1000 chars for description, 50KB payload cap), (6) orchestrator remains auth-agnostic (API layer owns all auth/validation logic). Auth flow: caller provides `X-API-Key` header → API validates against Key Vault-backed secret → rate-limit check → duplicate `runId` check via GitHub → ACA Job invocation with `runId`/`triggerSource`/`guidingDescription` → structured logs with correlation IDs. Rejection scenarios: missing/invalid API key (401), malformed input (400), rate limit exceeded (429), duplicate request (200 idempotent). Abuse prevention: daily/burst limits, input validation, idempotency enforcement, payload size cap. Future enhancements deferred to Tier 2: managed identity support, multi-tenant quotas, webhook callbacks, content moderation. Recommendation written to `workspace/kyle-issue-17-auth-contract-recommendation.md` for team review. Open questions for Max: API key distribution/rotation approach, multi-tenant scope, failure notification requirements, description safety validation.
- **2026-04-27 issue #17 auth decision closure (Scribe):** Inbox entries merged into `.squad/decisions.md`. Stan resolved auth decision: **API key Phase 1 MVP (Kyle's design) adopted; Entra ID deferred to Phase 2.** Decision rationale: API key simpler for single-user MVP, programmatic-friendly (CLI/script), lower lift (~1 week vs ~1-2 weeks), aligns with existing Key Vault infrastructure, sufficient security with RBAC audit trail. Entra ID appropriate for Phase 2 when multi-user or admin UI emerges. Orchestration log: `.squad/orchestration-log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Session log: `.squad/log/2026-04-27T17-23-22Z-issue-17-auth-decision.md`. Inbox files deleted (merged). Next: Kyle + Tolkien Phase 1 implementation; Butters acceptance matrix design; Stan Phase 1 review gate.
- **2026-04-27 clawpilot internal agent API recommendation:** Max requested backend/API contract design for internal agent caller "clawpilot" (based on openclaw) to trigger issue #17 manual generation without frontend exposure. Kyle recommended: POST `/api/internal/generate` with `requestId` (client-provided idempotency key, format `clawpilot-{date}-{uuid}`), `guidingDescription` (optional, max 1000 chars, passed as Curator context not prompt override), `callerIdentity` (fixed `clawpilot-agent`), `metadata` (triggerSource=internal-agent, correlationId for log tracing). Idempotency: API pre-checks GitHub gallery state for duplicate `runId` before job trigger, returns HTTP 200 `already_resolved` if found. guidingDescription handling: passed as `GUIDING_DESCRIPTION` env var to orchestrator, Curator includes in reasoning context for room selection, logged as boolean flag (not plaintext) to protect creative content. Caller identity binding: `requestId` transforms to `runId=manual-{runDate}-{uuid-suffix}`, gallery metadata includes `requestMetadata.callerIdentity` and `correlationId` for audit trail. Network isolation: not accessible from frontend via Private Endpoint (prod) or IP allowlist + header auth (MVP), zero CORS exposure. Auth: header-based `X-Clawpilot-Key` validated against Key Vault secret. Rate limits: 10 requests/day, 2 concurrent, 10-min cooldown per caller. Response shapes: HTTP 202 accepted with executionId, HTTP 200 idempotent duplicate, HTTP 400/401/429/503 for validation/auth/rate/trigger errors. Orchestrator contract: receives `RUN_ID`, `TRIGGER_SOURCE`, `GUIDING_DESCRIPTION`, `REQUEST_ID`, `CORRELATION_ID`, `CALLER_IDENTITY` env vars. Rollout: 2-3 weeks (API skeleton → ACA Job integration → production hardening). Recommendation written to `workspace/kyle-clawpilot-api-contract-recommendation.md`. Open questions: Key distribution approach, failure notification mechanism, concurrent run limit, multi-agent scope, description safety validation, gallery metadata visibility. Next: Max confirms open questions, Kyle implements `GUIDING_DESCRIPTION` handling in orchestrator, Tolkien builds API layer, Butters designs acceptance matrix.
- **2026-04-27 clawpilot agent decision merge:** Scribe merged all issue #17 clawpilot agent inbox entries into `.squad/decisions.md`. Max clarified API is for internal "clawpilot" agent (OpenClaw-based), not frontend. Kyle's API contract adopted: POST `/api/internal/generate`, `requestId`/`guidingDescription`/`callerIdentity` fields, `X-Clawpilot-Key` header auth, idempotency pre-check, `runId` derivation from `requestId`, multi-per-day same-`runDate` support. Auth responsibility: Kyle implements `GUIDING_DESCRIPTION` env var handling and orchestrator acceptance of optional guiding context; Tolkien builds Azure Function API layer with Key Vault integration; both own coordination on orchestrator CLI extension. Curator ownership preserved: `guidingDescription` passed as context input only, Curator retains final room assignment authority. Structured logging: `caller_identity`, `request_id`, `run_id`, `triggerSource` tracked; `guidingDescription` logged as boolean flag + char count (not plaintext) to protect creative content. Inbox entries: copilot-directive (user intent), kyle-clawpilot-api-contract (API spec), tolkien-clawpilot-agent-auth (auth approach) merged; deduped and deleted from inbox. Orchestration log: `.squad/orchestration-log/2026-04-27T17-35-52Z-issue-17-clawpilot-agent.md`. Session log: `.squad/log/2026-04-27T17-35-52Z-issue-17-clawpilot-agent.md`. Next: Phase 1 implementation kickoff (Kyle orchestrator extension, Tolkien API layer, ~1 week estimate); Butters acceptance matrix; Stan review gate.
- **2026-04-27 issue #17 API hosting and auth comprehensive decision (Scribe):** Four-agent comprehensive review. Stan's hosting recommendation: Azure Functions Consumption Plan chosen for clean HTTP boundary, managed identity support, serverless billing, Key Vault alignment. Kyle's API surface inventory: confirmed no current HTTP API; documented requirements if thin API chosen (Function App, POST `/api/internal/generate` route, `X-API-Key` auth, idempotency pre-check, orchestrator CLI `--guiding-description` extension, rate limits 10/day 2-concurrent 10-min cooldown). Cartman's doc assessment: identified gaps; recommended three-part plan (architecture, demo, learning docs) with Microsoft Learn citations. Tolkien's platform analysis: detailed four hosting options; Phase 1 rationale for Functions (separation of concerns, auth simplicity, operational clarity, idempotency guard, rate limiting, scalability). **Final decision: API key Phase 1 MVP; Entra ID Phase 2 deferred.** Kyle's Phase 1 deliverables: (1) extend orchestrator CLI to accept `--guiding-description` argument (optional, max 1000 chars), (2) pass to Curator role as context advisory, (3) accept `GUIDING_DESCRIPTION` environment variable from API layer, (4) log structured events with caller_identity and request_id. Cross-team coordination: Kyle + Tolkien ~1 week Phase 1 implementation; Butters acceptance matrix (API key validation, rate limiting, idempotency, concurrency, logs); Stan review gate. Orchestration logs: 2026-04-27T18-36-43Z-{stan|kyle|tolkien|cartman}.md. Session log: 2026-04-27T18-36-43Z-issue-17-api-design-question.md. Merged decisions: .squad/decisions.md entry 34. Inbox files deleted (5 total). Next: Kyle implements orchestrator extension; Tolkien builds Function App; Phase 1 proof cycle begins.


37. - **2026-04-27 clawpilot API key distribution & authentication handoff (Scribe session):** Max's blocker—"How would clawpilot provide the key? I don't know that i have a method for initiating that handoff"—resolved through comprehensive team consensus on Phase 1 + Phase 2 strategy. Kyle analyzed five practical auth paths: (1) Local/Dev API key in .env (Phase 1 MVP, recommended), (2) Key Vault retrieval by clawpilot (Phase 2+, requires Azure compute), (3) Managed identity + Entra bearer tokens (Phase 2+ recommended, zero-secret posture), (4) GitHub Actions OIDC (lower priority, complex), (5) Azure Functions built-in keys (discouraged, non-standard). **Kyle recommendation: Dual-mode API authentication from day 1** (Phase 1 X-Clawpilot-Key header, Phase 2 Authorization: Bearer tokens, no breaking changes). Phase 1 implementation: Kyle generates random 64-byte key, stores in Key Vault secret clawpilot-api-key; Max self-serves retrieval via Azure Portal or z keyvault secret show (never email/paste); Max stores locally in .env (gitignored). API contract: POST /api/internal/generate with header validation, request fields
equestId/guidingDescription/callerIdentity, response codes 202/200/401/429; rate limits 10 requests/day, 2 concurrent, 10-min cooldown. Inbox entries merged: cartman-auth-handoff-grounding.md (Microsoft Learn grounding), kyle-clawpilot-auth-handoff.md (five paths + dual-mode API), tolkien-clawpilot-auth-handoff.md (comparative matrix + Phase 1/2 decision). Orchestration logs: 2026-04-27-cartman.md, 2026-04-27-kyle.md, 2026-04-27-tolkien.md. Session log: 2026-04-27-clawpilot-auth-handoff.md. Decisions merged: .squad/decisions.md entry 35. Inbox files deleted (post-merge deduplication complete). Phase 1 deliverables: Kyle to generate API key and store in Key Vault; extend orchestrator CLI to accept optional --guiding-description argument; pass GUIDING_DESCRIPTION env var to Curator as context advisory; implement structured logging with caller_identity and request_id. Next: Kyle + Tolkien Phase 1 implementation (~1 week); Butters acceptance test matrix; Stan Phase 1 review gate before production deployment.


## 2026-04-27 Issue #17 Phase 1 implementation

- Added a minimal JavaScript Azure Functions app under api/ with POST /api/internal/generate, X-Clawpilot-Key validation against Key Vault secret clawpilot-api-key via DefaultAzureCredential, request validation for requestId and 1000-char guidingDescription, best-effort data/gallery.json duplicate precheck, and Container Apps Job start via @azure/arm-appcontainers with overrides for RUN_ID, TRIGGER_SOURCE=manual-api, REQUEST_ID, CALLER_IDENTITY, CORRELATION_ID, HOSTED_TRACE_ID, and GUIDING_DESCRIPTION.
- Extended orchestrator contracts and CLI/env handling for --guiding-description, trigger/request/caller/correlation context, Curator advisory manual guidance, and triggerSource persistence on published images/skips. Tightened runId-scoped validation paths for publish/skip transitions and fixed proof execution to use matching runId/defaults without bytecode churn.
- Documented the API contract and settings in docs/api/internal-generate.md. Validation passed: npm run api:build, npm --prefix api test, npm run build, and npm run orchestrator:proof.

- **2026-04-27 issue #17 Phase 1 API delivery completion:** Kyle's backend implementation (POST /api/internal/generate) approved by Butters. All blockers resolved: (1) distinct same-day manual runs now publish independently (IDEM-2 fixed via runId-scoped validation), (2) auth validation prevents job invocation on missing/wrong X-Clawpilot-Key, (3) duplicate requests return HTTP 200 already_resolved without triggering job. Environment variable passing confirmed: REQUEST_ID, CALLER_IDENTITY, CORRELATION_ID, GUIDING_DESCRIPTION correctly passed to orchestrator. Logging sanitization verified: guiding description logged as length+boolean only, never raw text. Code-level unit tests cover auth validation, gallery duplicate detection, missing-key rejection, scheduling compatibility. Deployment-ready: infrastructure provisioned by Tolkien (Azure Functions Flex Consumption with managed identity, Key Vault integration, RBAC roles). Next: hosted smoke proof (DEPLOY-1–4) and acceptance test matrix before production enablement.
- **2026-05-01 critique duplicate failure:** ACA manual run `manual-2026-05-01-manual-20260501-lobster-003` failed because `CriticRole` critiques the latest existing gallery image with `CritiqueEntry.id=latest_image.id`; that latest image already had a persisted critique, and `apply_publish_outcome()` appended another same-id critique before `validate_critiques_state()` rejected it. Retry/manual reruns will keep failing until code skips/reuses existing critiques or appends critiques idempotently; current checked-in data is valid and needs no repair unless orphan 2026-05-01 assets or duplicate critique rows were persisted elsewhere.
- **2026-05-01 duplicate critique backend fix:** Implemented the accepted contract fix for ACA execution `artclaw-daily-job-2pewehrfleuls-m0011pb`: `execute_role_steps()` now skips Critic when `gallery.latest_image().id` already exists in `data/critiques.json`, preserving token budget and avoiding non-persistable duplicate critique output. Added `append_critique_if_new()` as a defensive publish/skip append guard so duplicate critique IDs are no-op sidecar writes while `validate_critiques_state()` still rejects real duplicates already present in persisted state. Regression proof now seeds controlled gallery/critiques fixtures and covers `latest-image-already-critiqued-publish` plus `duplicate-critiques-pre-run`; validation passed with `npm run orchestrator:proof` and `npm run build`.
- **2026-05-01 duplicate critique Scribe consolidation:** Kyle's skip-existing-critique contract, defensive append guard, and proof coverage were merged into `.squad/decisions.md`; Butters approved after proof/build/current-data dry-run.
- **2026-05-01 room capacity failure:** ACA execution `artclaw-daily-job-2pewehrfleuls-wl2kbju` ran image `evalt1acr.azurecr.io/artofclawpilot-runner:20260501202801` and failed with `room_capacity_exceeded` (`room-01`, `count=6`) after remote main already had five images in the only room. This is not the duplicate-critique failure; retrying before adding room-capacity handling will repeat after costly Foundry calls, while the failed run left no committed gallery/critiques/assets record for its runId.

- **2026-05-01 wl2kbju Scribe consolidation:** Decisions now record this as a new room-capacity contract failure. Backend next work is pre-generation full-room handling and regression proof; retry remains valid only after zero-call skip or safe room rollover/capacity policy is implemented.

- **2026-05-01 safe-room capacity fix:** Added post-Curator/pre-Critic room-capacity resolution. If the selected room is full, the orchestrator deterministically rolls over to the available room with the fewest images and stable room id; if every room is full, it records a non-retryable structured skip before Critic, Artist, or image generation. Artist guidance now requires original wall-display artworks/panels/canvases/digital works, draws only broad inspiration from historical artists/movements, and avoids statue/object/architecture/photo-of-object prompts unless explicitly requested. Proof coverage added for selected-room-full rollover and all-rooms-full zero-call skip; validation passed `npm run orchestrator:proof` and `npm run build`.
